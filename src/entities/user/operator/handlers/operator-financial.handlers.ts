export const operatorFinancialHandlers = [
	// http.get(
	// 	`${BASE_URL}${OPERATOR_PATHS.getOperatorFinancials.url}`,
	// 	async () => {
	// 		await delay(500);
	// 		return HttpResponse.json(financialStore, { status: 200 });
	// 	}
	// )
	// http.patch(`${BASE_URL}${OPERATOR_PATHS.updateOperatorFinancials.url}`, async ({ request }) => {
	// 	await delay(500);
	// 	const body =
	// 		(await request.json()) as TOperatorFinancialSettingsUpdateBackend;
	// 	financialStore = {
	// 		...financialStore,
	// 		...body,
	// 	};
	// 	return HttpResponse.json(financialStore, { status: 200 });
	// })
];
