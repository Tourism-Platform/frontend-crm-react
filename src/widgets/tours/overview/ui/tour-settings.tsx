import { type FC } from "react";

import { Card, CardContent } from "@/shared/ui";

export const TourSettings: FC = () => {
	return (
		<Card>
			<CardContent>TourSettings</CardContent>
		</Card>
	);
};
